services:
  - type: web
    name: nafas-kitchen-minute
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: nafas-kitchen-db
          property: connectionString

databases:
  - name: nafas-kitchen-db
    plan: free
